version: '3.8'
services:
  hasii:
    build: .
    container_name: mandirmusic_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./MandirMusic/cookies:/app/MandirMusic/cookies
      - ./downloads:/app/downloads
    command: bash start
